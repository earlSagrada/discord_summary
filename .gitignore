# Python virtual environments
.venv/
venv/
ENV/
env/

# Python cache/artifacts
__pycache__/
*.py[cod]
*.pyo

# Local tool cache
data/cache/

# yfinance 行情当日缓存（可随时删，会自动重拉）
data/market_cache/

# Local inbox (browser downloads land here, then get filed into data/chats_by_date/)
data/inbox/

# Runtime log of the 15-min cycle (diagnostics only, not source)
data/cycle.log
data/cycle_state.json
# Substack 校准流水线的运行日志与状态
data/pipeline.log
data/substack_seen.json
# AI 调过的参数覆盖层 + 备份（删掉即回到 config.py 的出厂默认）
data/tunables.json
data/tunables.bak.json
# VIP 名单的连续期数计数与备份
data/vip_state.json
data/vip_speakers.bak

# Generated by review.py (regenerable reports / suggestions, not source)
trade_notes/reviews/
prompts/vip_suggestions.md
# Generated by optimize.py（决策记录：可再生，但想长期留档就把这行删掉）
trade_notes/optimizations/

# Frank 的 Substack 复盘（付费订阅内容，只在本地做方法论学习，不进仓库）
data/substack_post/

# Local secrets/config
.env
.env.*
!.env.example

# Generated Discord exports (keep scripts/source files only)
# discord-*.txt
discord-*.json
# discord-*.md

# OS/editor noise
.DS_Store
Thumbs.db
